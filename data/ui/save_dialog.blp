using Gtk 4.0;
using Adw 1;

template $GradienceSaveDialog : Adw.AlertDialog {
  heading: _("Save preset as…");
  body-use-markup: true;

  [extra-child]
  Entry preset-entry {
    placeholder-text: _("Preset Name");
  }
}
